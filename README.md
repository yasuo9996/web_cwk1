# web_cwk1

A music data analysis API based on acoustic features, without using large models, but using pure mathematical calculations (cosine similarity, aggregation statistics) to provide music insights. It also integrates complete CRUD to record user behaviors and is deployed on modern cloud platforms.
